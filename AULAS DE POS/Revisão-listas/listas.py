# criando uma lista
listas = ['a', 'b', 'c', 'd', 'e']
# adicionando elemento a lista
listas.append('f')
print(listas)
# remover itens da lista
listas.remove('e')
# tamanho das listas

print(len(listas))
# lista em for
for lista in listas:
    print(lista)
